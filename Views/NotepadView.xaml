<Window x:Class="SystemManager.Views.NotepadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <!-- Цвет окна -->
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuBarBrushKey}" Color="#2D2D30"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#2D2D30"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#F1F1F1"/>
        
        <!-- Цвет при наведении мыши -->
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#094771"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF"/>
        
        <!-- Цвет при клике -->
        <SolidColorBrush x:Key="{x:Static SystemColors.ActiveBorderBrushKey}" Color="#3F3F46"/>
        
        <!-- Стиль для Menu -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
        </Style>
        
        <!-- Стиль для MenuItem -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="Background" Value="#2D2D30"/>
        </Style>
        
        <!-- Стиль для Separator в меню -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" Command="{Binding SaveAsCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Меню -->
        <Menu DockPanel.Dock="Top" Background="#2D2D30">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Новый" InputGestureText="Ctrl+N" Command="{Binding NewCommand}"/>
                <MenuItem Header="_Открыть..." InputGestureText="Ctrl+O" Command="{Binding OpenCommand}"/>
                <Separator/>
                <MenuItem Header="_Сохранить" InputGestureText="Ctrl+S" Command="{Binding SaveCommand}"/>
                <MenuItem Header="Сохранить _как..." InputGestureText="Ctrl+Shift+S" Command="{Binding SaveAsCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Статус-бар -->
        <StatusBar DockPanel.Dock="Bottom" Background="#007ACC" Foreground="White" Padding="5,2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusInfo}" FontFamily="Segoe UI" FontSize="12"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Редактор -->
        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 Background="#1E1E1E"
                 Foreground="#D4D4D4"
                 CaretBrush="White"
                 BorderThickness="0"
                 Padding="10"/>
    </DockPanel>
</Window>